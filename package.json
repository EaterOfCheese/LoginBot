{
  "name": "discord-login-bot",
  "version": "1.0.0",
  "description": "Login bot, actions performed for event",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "IV",
  "license": "ISC",
  "dependencies": {
  },
  "devDependencies": {
  }
}
